const initialState = {}

const ActionType = {

}

//@ts-ignore
const appReducer = (state = initialState, action:ActionType) => {
    switch (action.type) {
        default: return state
    }
}

export default appReducer;